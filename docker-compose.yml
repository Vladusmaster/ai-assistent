services:
  ai-assistant:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reu_ai_assistant
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env